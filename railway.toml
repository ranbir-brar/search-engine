[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn search_api:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 100

[[services]]
name = "atlas-api"
